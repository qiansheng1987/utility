spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/testmybatis?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: com.example.entity
  mapperLocations: classpath:mappers/*.xml
